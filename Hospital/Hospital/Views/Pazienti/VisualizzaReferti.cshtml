@model IEnumerable<Hospital.Models.referto>

@{
    ViewBag.Title = "VisualizzaReferti";
}

<h2>VisualizzaReferti</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdReferto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descrizione)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.IdReferto)
    </td>

    <td>
        @Html.DisplayFor(modelItem => item.Descrizione)
    </td>
</tr>
}

</table>
